# Mock/example values only. Do not use these values for a real deployment.
# Real AWS credentials must be configured in HCP Terraform as Environment variables.

AWS_ACCESS_KEY_ID=AKIA_MOCK_REPLACE_WITH_REAL_VALUE
AWS_SECRET_ACCESS_KEY=mock_secret_replace_with_real_value
AWS_DEFAULT_REGION=us-east-1
